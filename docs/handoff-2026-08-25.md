# 项目交接文档 · 2026-08-25

**交接时间**：2026-08-25 晚。
**性质**：取代 `docs/handoff-2026-08-24.md` 中的一切「现状」描述（该文的六题配对采集部分已全部签入，合并路线部分已因 PR 清零而失效）。仓库内权威状态见 `STATUS.md`。

---

## 一句话现状

**机制已全部落地（采集 → trace → 审计 → 强制），六题配对 6/6 完成且全部通过独立审计，open PR 归零。卡在正式配对样本 6/30、有效 oracle 失败样本 0/10，评估仍是 `evidence_insufficient`、`efficiency_claim: not_supported`。下一个动作是修 #58 闸门缺陷——它是所有扩量的前提。**

## 项目在干什么（给不熟悉的人）

研究一个问题：AI 编码代理会重复测试、扩大测试、该停不停。本项目给验证行为立一套分级规矩（off / smoke / standard / thorough 四档），然后用对照实验证明它有效——同一道题、同一模型各跑两遍，唯一差别是有没有 hook 执行规矩。结论有硬门槛：30 组配对比较、10 个有效失败样本、2 类泛化场景。没到门槛之前，一切效率声明都不许做。

## 当前仓库事实

| 项 | 值 |
| --- | --- |
| **活跃工作树** | `/Users/qianyuhe/Documents/ChatGPT/llm test` |
| **分支 / 提交** | `codex/publish-research` @ `cd5ab47`，与 `origin` 一致（ahead/behind 均为 0） |
| **工作区** | 干净，零未提交改动，无 stash |
| **远端最新提交** | `cd5ab47`（2026-08-25「docs: point STATUS.md at #58 as the hard blocker」） |
| **open PR** | **0**（#49–#56 全部已合并） |
| **open issue（5）** | **#58**（闸门缺陷）、**#59**（pino 出题）、#17、#16、#1（roadmap） |
| **npm run check** | 173 通过 / 0 失败（2026-08-25 实跑确认） |
| ⚠️ | `~/Documents/GitHub/ai-coding-agent-test-strategy` 是 08-14 的僵尸副本（停在 `e2a2fd2`，6 个脏文件），别按目录名找盘 |

## 自上一份交接（08-24）以来发生了什么

1. **六题配对 run 正式签入**：`fixtures/benchmark/verification-policy-run-2026-08-24/`（12 trace + 12 oracle + audit/run report）。handoff-08-24 里「所有改动都没提交」「三个 script 未跟踪」的问题已全部解决——脚本进了库，doc 按行号引用的证据不再悬空。
2. **stacked PR 清空**：#55（外部仓库任务源）+ #56（L2 改写档）先后合并进默认分支。
3. **#57 落地**（`9c7c9c9`）：oracle 新增显式 `undecided` 结论与 `post_run_decidable` 字段。原来那条「结构性恒 failed」的假失败样本被诚实撤掉；审计器不信声明，自行重算四个条件（没给理由、没说明改由谁判、居然执行过命令、还在报失败签名——任一即判不可用），伪造会被拦。`scripts/audit-verification-runs.mjs` 由此获得首批测试。
4. **STATUS.md 首次对齐**（`57cfc35`），随后开出 **#58**、**#59**，关闭 #57，更新 #1 路线图。
5. **STATUS.md 自相矛盾修掉**（`cd5ab47`）：`57cfc35` 只改了正文，漏了开头第 5 行和 issue 名册，导致头部仍写「硬阻塞只有一个 #57」而正文写 #57 已落地。现在头部指向 #58 并带上实测代价，名册补全 5 个 open issue 与 #57 / #43 的关闭记录。
6. 会话链：1–4 由 Claude Code 会话 `6e50c664`（续 `1987b7c0`）完成；08-25 下午会话 `d33e045d` 全景扫描；第 5 条与本文签入由 08-25 晚间会话完成。

## 四条硬门槛对账（扩量前逐条盯）

| 门槛 | 现状 | 说明 |
| --- | --- | --- |
| 配对比较 ≥ 30 | **6/30** | 质量全合格，纯数量问题，可靠采集堆出来 |
| 有效 oracle 失败样本 ≥ 10 | **0/10** ← 最难的坎 | 唯一候选 `vp_flaky_retry_once` 结构上判不了（flaky 标记首跑即消耗），已如实改 `undecided`。要靠其余五题出现「因正确原因失败」的 oracle 或新题满足；**不能靠数量凑，也不能造假** |
| 泛化场景类 ≥ 2 | **1/2** | 现有 6 题全是本仓库历史题（cli_tool）。pino / zustand / yargs 已过环境资格，但**出题才算数**，fixture 资格 ≠ 观测到行为 |
| 外部仓库出题 | **0/3** | 同上；第一道就是 #59（pino） |

## 当前阻塞与执行顺序

1. **修 #58（必修，排第一）**。闸门用整串正则匹配，决策却按段分解，两把尺子不一致：一条 `rg` 只要模式里出现 `pytest` 就被当成测试命令，逐段找不到 runner 就 fail-closed 拒掉。代价已精确计算：`vp_unknown_impact_full_fallback` 过闸 31 次、白丢 5 次。**不修的话，之后攒的每一对都掺同样的噪声。**
2. **#59：在 pino 上写出第一道外部仓库任务** → 场景类 2/2 即达标。选 pino 不是随便挑：它是 `service_library`，一道题同时解掉「场景覆盖 1/2」和「样本自指」，跑通即验证外部出题这条流水线。
3. **继续配对采集至 30 对**（harness 基准：`codex-cli@0.149.0` / `gpt-5.6-sol` / effort high；baseline 臂 policy `unmanaged-coding-agent-baseline@1`，candidate 臂 hook + `observatory-verification-policy@0.4-unscoped`）。
4. **30 对之后才跑 oracle safety gate**；在此之前不做任何效率宣传、不迁移 DSH。

## 家务清单

**已完成，不用再动**（08-25 核对）：

- ~~#43 可以关了~~ **已于 2026-08-24 15:47 关闭**，`unscoped_test_command_denied` 真机触发的签入证据齐备。
- ~~#1 路线图有 4 个已完成未勾的框~~ **#38 / #40 / #44 / #45 均已勾选**。当前未勾选项就是 #58 与 #59 两个待办本身。

**仍需留意**：

- 原始采集目录 `/tmp/vp-collect-2026-08-24` **易失**（重启即没，08-25 晚仍在，77522 个文件）。doc 引用的一切都能从已签入 fixture 复算，但要 `--rebuild-trace` 就得趁它还在。
- 本地有 24 个已完整合并的 `codex/*` 分支可安全删除（`git branch -d`）；另有 `codex/decompose-compound-shell`、`codex/observe-shell-file-writes` 未合并，删除需 `-D`，勿顺手清。

## 证据纪律（接手前必读，违反即返工）

1. **资格由审计器从原始证据重推，不采信采集器自述**——包括新加的 `undecided`，声明本身也要过审。
2. **doc 里任何一句话不许靠没签入的证据支撑**。这条真的拦下过一次错误发布。
3. 外部 fixture 考察 **3 过 6 拒，拒绝理由全部在案**（如 commander 在 `CI=1 NO_COLOR=1` 下 2/1373 属环境冲突，未打补丁——为单个 fixture 放宽 runner 环境会让它和其他 fixture 不可比）。别为凑数放宽。

## 权威文档顺序

`STATUS.md` ≈ `docs/verification-policy-benchmark-v0.1.md` > `SESSION.md`（追加式检查点日志，只加不改）> 本文 > 历史 handoff（`docs/handoff-2026-08-24.md`、`docs/handoff-2026-08-19.md` 仅存历史价值）。
