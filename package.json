{
  "name": "ai-coding-agent-test-strategy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check src/verifier.mjs && node --check src/cli.mjs && bash -n scripts/verify.sh && npm test",
    "test": "node --test test/*.test.mjs",
    "replay": "node src/replay-cli.mjs",
    "verify": "node src/cli.mjs"
  }
}
